doctrine:
    dbal:
        default_connection: default
        connections:
            default:
                driver: pdo_mysql
                dbname: hautelook_alice_bundle
                port: 3307
                user: root
                password: password
    orm:
        auto_mapping: true
